---
const { name, role, imageSrc } = Astro.props;
---
<div class="text-center">
  <img src={imageSrc} alt={`${name}`} class="mx-auto w-24 h-24 rounded-full mb-4" />
  <h3 class="font-semibold">{name}</h3>
  <p class="text-gray-600">{role}</p>
</div>
